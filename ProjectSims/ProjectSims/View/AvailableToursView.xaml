<Window x:Class="ProjectSims.View.AvailableToursView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectSims.View"
        mc:Ignorable="d"
        Title="Tour Tracking View" Height="450" Width="700" WindowStartupLocation="CenterScreen">
        <Grid>
            <DataGrid  x:Name="AvailableTour" Width="600" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,30,0,0"
                       ItemsSource="{Binding AvailableTours}"
                       SelectionMode="Single" 
                       SelectedItem="{Binding Path=SelectedTour}" IsReadOnly="True" Height="254" 
                       AutoGenerateColumns="False" >
                 <DataGrid.Columns>
                    <DataGridTextColumn Header="Dostupne ture" Binding="{Binding Name}" Width="400"/>
                    <DataGridTextColumn Header="Početak" Binding="{Binding StartOfTheTour}" Width="200"/> 
            </DataGrid.Columns>
             </DataGrid>
        <Button Click="StartTour_Click" Content="Započni turu" HorizontalAlignment="Left" Margin="550,300,0,0" VerticalAlignment="Top" Height="30" Width="100" Background="#061950" Foreground="White" FontWeight="Bold"/>
    </Grid>
 


</Window>
