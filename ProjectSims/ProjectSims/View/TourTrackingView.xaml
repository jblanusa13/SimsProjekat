<Window x:Class="ProjectSims.View.TourTrackingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectSims.View"
        mc:Ignorable="d"
        Title="Tour Tracking View" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <TextBox x:Name="TourInfoTextBox" HorizontalAlignment="Left" Margin="20,20,0,0" VerticalAlignment="Top" Width="750" Height="40" FontSize="20" FontWeight="Bold" BorderThickness="0" IsEnabled="False"/>
        <ListView x:Name="KeyPoints" Width="700" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,50,0,0" ItemsSource="{Binding KeyPointList}">
            <ListView.View>
                <GridView>
                    <GridViewColumn>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox Margin="10,10,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" IsChecked="{Binding isChecked}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Margin="30,10,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Text="{Binding Name}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>





    </Grid>
</Window>
