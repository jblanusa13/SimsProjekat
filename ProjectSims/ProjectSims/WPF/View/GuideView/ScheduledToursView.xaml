<Window x:Class="ProjectSims.View.GuideView.ScheduledToursView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectSims.View.GuideView"
        mc:Ignorable="d"
        Title="ScheduledToursView" Height="700" Width="1000">
    <Grid>
        <DataGrid  x:Name="ScheduledTour" Width="700" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,60,0,0"
                       ItemsSource="{Binding ScheduledTours}"
                       SelectionMode="Single" 
                        Background="Transparent"
                       SelectedItem="{Binding Path=SelectedTour}" IsReadOnly="True" Height="400" 
                       AutoGenerateColumns="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Zakazane ture" Binding="{Binding Name}" Width="400"/>
                <DataGridTextColumn Header="Početak" Binding="{Binding StartOfTheTour}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Click="CancelTour_Click" Content="Otkazi turu" HorizontalAlignment="Left" Margin="200,500,0,0" VerticalAlignment="Top" Height="30" Width="200" Background="#061950" Foreground="White" FontWeight="Bold"/>
    </Grid>
</Window>
