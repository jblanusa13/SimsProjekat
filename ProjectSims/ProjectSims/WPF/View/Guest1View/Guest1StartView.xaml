<Window x:Class="ProjectSims.View.Guest1View.Guest1StartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectSims.View.Guest1View"
        mc:Ignorable="d"
        MinHeight="600" MinWidth="1067"
        Title="Smestaji" Height="600" Width="1067"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Frame x:Name="WindowBarFrame" NavigationUIVisibility="Hidden"></Frame>
        </Grid>

        <Grid Grid.Row="1">
            <Frame x:Name="SelectedTab" NavigationUIVisibility="Hidden" ></Frame>
        </Grid>

        <Grid Grid.Row="2">
            <Frame x:Name="StatusBarFrame" NavigationUIVisibility="Hidden"></Frame>
        </Grid>
    </Grid>
</Window>