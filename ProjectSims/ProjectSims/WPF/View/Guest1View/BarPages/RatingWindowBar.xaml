<Page x:Class="ProjectSims.WPF.View.Guest1View.BarPages.RatingWindowBar"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjectSims.WPF.View.Guest1View.BarPages"
      mc:Ignorable="d" 
      d:DesignHeight="30" d:DesignWidth="700"
      Title="RatingWindowBar">

    <Grid Background="{DynamicResource BarShade}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Foreground="{DynamicResource BarFont}"
                    FontFamily="Bahnschrift" FontSize="15"
                   Margin="10,7">TravelAndTour</TextBlock>

        <Button Grid.Column="4" Name="CloseButton" BorderBrush="{DynamicResource BarShade}" Background="{DynamicResource BarShade}"
                Click="Close_Click"
                FocusVisualStyle="{DynamicResource FocusControl}">
            <DynamicResource ResourceKey="RatingsCloseIcon"/>
        </Button>
    </Grid>
</Page>
